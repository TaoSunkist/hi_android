<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:padding="@dimen/margin_small"
    app:layout_anchorGravity="center">

    <Button
        android:id="@+id/cell_loading_retry_button"
        style="@style/defaultGradientButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:minHeight="32dp"
        android:visibility="invisible" />

    <ProgressBar
        android:id="@+id/cell_loading_progress_bar"
        android:layout_width="@dimen/progress_bar_indeterminate_small"
        android:layout_height="@dimen/progress_bar_indeterminate_small"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:progressTint="@color/colorAccent"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/cell_loading_status_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textSize="14sp"
        android:visibility="invisible" />

</FrameLayout>